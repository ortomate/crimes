[
  {
    "id": "review-01-dst-arithmetic",
    "fixture": "01",
    "kind": "review",
    "prompt": "A PR adds a `tomorrowMs` helper to src/date.ts that adds 86_400_000 to a timestamp. Use `crimes scan src/date.ts --format json` to find the DST-naive arithmetic finding. Explain whether you'd block the PR or request changes, citing the finding's evidence verbatim. Identify which two dates per year the helper would produce a wrong answer.",
    "expected_artifacts": {
      "referenced_findings": ["dst_naive_arithmetic"],
      "referenced_files": ["src/date.ts"],
      "expected_priority": "dst_naive_arithmetic"
    },
    "judge_questions": [
      "Did the agent quote the day-level constant (86400000 or `day` unit) from the finding evidence verbatim?",
      "Did the agent name the two DST-transition days as the failure window rather than treating the bug as universal?"
    ]
  },
  {
    "id": "review-01-messy-ts-app",
    "fixture": "01",
    "kind": "review",
    "prompt": "Imagine a PR added the current state of this repo on top of an empty baseline. Use `crimes scan --format json` to enumerate findings and categorise each as: block merge / warn / ignore. Justify each category with the finding's severity + scores.agent_risk.",
    "expected_artifacts": {
      "referenced_findings": ["large_function", "direct_date", "todo_density"],
      "expected_priority": "large_function"
    },
    "judge_questions": [
      "Did the agent's block/warn/ignore split align with severity (high \u2192 block, medium \u2192 warn, low \u2192 ignore)?",
      "Did the agent justify deviations by citing scores.agent_risk specifically?"
    ]
  },
  {
    "id": "review-05-stress-ia-drift",
    "fixture": "05",
    "kind": "review",
    "prompt": "Review the IA consistency across src/routes/, src/nav.ts, and docs/routes.md using `crimes scan --format json`. Identify the drift findings and recommend whether they're blocking-quality issues or follow-ups.",
    "expected_artifacts": {
      "referenced_findings": ["concept_alias_drift", "orphaned_destination"],
      "referenced_files": [
        "src/routes/teams.ts",
        "src/routes/workspace.ts",
        "docs/routes.md"
      ]
    },
    "judge_questions": [
      "Did the agent recognise that the same entities are referred to inconsistently across files?",
      "Did the agent suggest a single normalised vocabulary rather than per-file patches?"
    ]
  },
  {
    "id": "review-02-react-dashboard",
    "fixture": "02",
    "kind": "review",
    "prompt": "Imagine a PR landed this dashboard codebase on top of an empty baseline. Use `crimes scan --format json` to enumerate the petty / single-file findings (dead code, magic literals, repeated role checks) and categorise each as: block merge / warn / ignore. Justify each category with the finding's severity + scores.agent_risk and group your output by detector type.",
    "expected_artifacts": {
      "referenced_findings": [
        "commented_out_code",
        "magic_domain_literal_scatter",
        "duplicated_role_status_plan_check"
      ],
      "expected_priority": "duplicated_role_status_plan_check"
    },
    "judge_questions": [
      "Did the agent treat duplicated_role_status_plan_check as the highest-leverage finding (it raises agent_risk because two sources of truth for the same predicate)?",
      "Did the agent recommend deleting commented_out_code rather than 'reviewing later'?"
    ]
  },
  {
    "id": "review-03-node-cli-tool",
    "fixture": "03",
    "kind": "review",
    "prompt": "Imagine a PR added this Commander.js codebase on top of an empty baseline. Use `crimes scan --format json` to enumerate the agent-risk findings \u2014 dead code, prose-rules-in-comments, magic literals, tests with weak assertions, large files. For each, decide block merge / warn / ignore, and justify with severity + scores.agent_risk.",
    "expected_artifacts": {
      "referenced_findings": [
        "commented_out_code",
        "logic_in_comments",
        "weak_test_signal",
        "magic_domain_literal_scatter"
      ],
      "expected_priority": "logic_in_comments"
    },
    "judge_questions": [
      "Did the agent flag logic_in_comments as something to convert into code or types, rather than just deleting the comment?",
      "Did the agent recognise that weak_test_signal in the Commander test suite is a coverage problem, not a style nit?"
    ]
  },
  {
    "id": "review-05-permission-and-parallel",
    "fixture": "05",
    "kind": "review",
    "prompt": "The /admin destination is described three different ways across nav, route guard, and docs. There's also a parallel destination (src/routes/admin/billing-plans.ts mirrors the top-level billing-plans route) and a docs page referencing iaq subcommands that the bin doesn't advertise. Use `crimes scan --format json` to find these three IA drifts. For each, recommend block merge / warn / ignore and explain which one carries the highest agent-risk and why.",
    "expected_artifacts": {
      "referenced_findings": [
        "permission_ia_drift",
        "parallel_destination",
        "command_drift_docs_code_drift"
      ],
      "referenced_files": [
        "src/routes/admin.ts",
        "src/nav.ts",
        "src/routes/admin/billing-plans.ts",
        "docs/routes.md"
      ],
      "expected_priority": "permission_ia_drift"
    },
    "judge_questions": [
      "Did the agent treat permission_ia_drift as a blocking-quality issue (access policy disagreement is a security risk, not a copy nit)?",
      "Did the agent recognise parallel_destination as the kind of finding that an agent extending one route will silently leave the other stale?"
    ]
  },
  {
    "id": "review-01-ia-drift",
    "fixture": "01",
    "kind": "review",
    "prompt": "Three IA findings overlap in this repo: a route's path / file / component / nav entries disagree (route_metadata_drift), one destination shows up in two different nav source files with different labels (duplicated_navigation_source), and a docs page links to a file that doesn't exist (docs_code_drift). Use `crimes scan --format json` to enumerate these and recommend whether each is a block merge / warn / ignore.",
    "expected_artifacts": {
      "referenced_findings": [
        "route_metadata_drift",
        "duplicated_navigation_source",
        "docs_code_drift"
      ],
      "referenced_files": [
        "src/nav/registry.ts",
        "src/routes/settings/billing.tsx",
        "docs/teams.md"
      ],
      "expected_priority": "route_metadata_drift"
    },
    "judge_questions": [
      "Did the agent point out that route_metadata_drift carries the highest agent_risk because the wrong-labelled surface is the one an extending agent reads first?",
      "Did the agent propose fixing the broken docs link by either creating the missing file or removing the reference, rather than 'flagging for later'?"
    ]
  },
  {
    "id": "review-01-hardcoded-localhost",
    "fixture": "01",
    "kind": "review",
    "prompt": "A PR adds an `adminUrl()` helper to src/api/files.ts that returns the admin dashboard URL. Use `crimes scan src/api/files.ts --format json` to find the `hardcoded_localhost` finding. Quote the literal URL from the finding's evidence and explain whether you'd block the PR or request changes. Recommend the specific configuration mechanism (env var name, settings module location, framework runtime-config slot) the URL should move to \u2014 don't stop at 'use config'.",
    "expected_artifacts": {
      "referenced_findings": ["hardcoded_localhost"],
      "referenced_files": ["src/api/files.ts"],
      "expected_priority": "hardcoded_localhost"
    },
    "judge_questions": [
      "Did the agent quote the literal URL (e.g. `localhost:3000`) from the finding evidence rather than paraphrasing 'a hardcoded URL'?",
      "Did the agent name a specific replacement mechanism (`process.env.ADMIN_URL`, a settings module path, etc.) rather than stopping at the generic 'move to config' recommendation?"
    ]
  },
  {
    "id": "review-01-oversized-raster",
    "fixture": "01",
    "kind": "review",
    "prompt": "A PR adds `src/assets/hero-banner.png` as the new landing-page hero. Use `crimes scan --format json` to find the `oversized_raster` finding. Quote the file size from the finding evidence and decide whether to block the PR or request changes. Recommend the specific transformation \u2014 target dimensions for the visible render area, and which modern format (WebP vs AVIF) you'd use for re-encode and why.",
    "expected_artifacts": {
      "referenced_findings": ["oversized_raster"],
      "referenced_files": ["src/assets/hero-banner.png"],
      "expected_priority": "oversized_raster"
    },
    "judge_questions": [
      "Did the agent quote the actual byte size / KB number from the finding evidence rather than paraphrasing 'too big'?",
      "Did the agent recommend a specific modern format (WebP or AVIF) and justify the choice (browser support, file-size vs quality tradeoff) rather than stopping at 'compress it'?"
    ]
  },
  {
    "id": "review-11-py-async-blocking",
    "fixture": "11",
    "kind": "review",
    "prompt": "This service falls over under moderate load: p99 latency collapses even though CPU stays low. Use `crimes scan --format json` to review the HTTP layer and identify what is causing it. Rank what you find by how much it contributes to the problem.",
    "expected_artifacts": {
      "referenced_findings": ["sync_io_in_hotpath"],
      "referenced_files": ["api/routes.py"],
      "expected_priority": "sync_io_in_hotpath"
    },
    "judge_questions": [
      "Did the agent identify blocking calls inside async def handlers as the cause?",
      "Did the agent explain that a blocking call in an async handler stalls the event loop for all concurrent requests, not just one worker?",
      "Did the agent propose async equivalents (httpx.AsyncClient, aiofiles, asyncio.sleep) or moving the work off the request path?"
    ]
  },
  {
    "id": "review-12-py-hollow-tests",
    "fixture": "12",
    "kind": "review",
    "prompt": "We are about to refactor this billing package and want to know how much the existing test suite actually protects us. Run `crimes scan --format json` and report which parts of the package are genuinely covered and which only look covered. Be specific about the difference.",
    "expected_artifacts": {
      "referenced_findings": ["weak_test_signal"],
      "referenced_files": ["tests/test_reporting.py", "billing/reporting.py"],
      "expected_priority": "weak_test_signal"
    },
    "judge_questions": [
      "Did the agent notice that the reporting tests call the code but assert nothing?",
      "Did the agent distinguish between a module having a test file and being meaningfully tested?",
      "Did the agent avoid claiming the package is well tested purely because test files exist?"
    ]
  },
  {
    "id": "review-13-polyglot-api-contract",
    "fixture": "13",
    "kind": "review",
    "prompt": "Review the contract between the web client and the billing API in this monorepo. Use `crimes scan --format json`. Report every place the two sides disagree, and rank them by how they would fail in production.",
    "expected_artifacts": {
      "referenced_findings": ["cross_language_route_drift", "cross_language_type_drift"],
      "referenced_files": [
        "packages/web/src/workspace-client.ts",
        "packages/api/billing/routes.py"
      ],
      "expected_priority": "cross_language_route_drift"
    },
    "judge_questions": [
      "Did the agent find both the missing route and the HTTP method mismatch?",
      "Did the agent distinguish the 404 failure mode from the 405 one?",
      "Did the agent avoid claiming a disagreement that crimes did not report?"
    ]
  },
  {
    "id": "review-04-swallowed-errors",
    "fixture": "04",
    "kind": "review",
    "prompt": "This build tool reports success on some failed builds and CI has been green through a broken release. Use `crimes scan src --format json` to find the swallowed_error findings, then say for each one whether the caught error is genuinely recoverable or whether discarding it is what hides the failure. Name the single site you would fix first and write the replacement catch block. Quote each finding's evidence rather than paraphrasing it.",
    "expected_artifacts": {
      "referenced_findings": ["swallowed_error"],
      "referenced_files": ["src/api-extractor.ts", "src/tsc.ts", "src/rollup.ts"],
      "expected_priority": "swallowed_error"
    },
    "judge_questions": [
      "Did the agent distinguish the three build-runner sites (runRollup / runTypeScriptCompiler / runDtsRollup), which log without the error object, from src/utils.ts jsoncParse, which returns {} and tells the caller nothing failed at all?",
      "Did the agent's replacement catch block preserve the original error \u2014 rethrow, `cause`, or a non-zero exit \u2014 rather than only improving the log message?"
    ]
  },
  {
    "id": "review-01-contract-drift",
    "fixture": "01",
    "kind": "review",
    "prompt": "This repo declares `User` twice and `Invoice` twice, and the two halves of each pair do not agree. Use `crimes scan --format json` to find them. Lead with the finding that names the disagreement, then say: which of the two files an agent adding a field would most likely edit, what silently breaks if it edits only that one, and where this should sit relative to the repo's structural findings (the large file, the large function). Justify that ordering with scores.agent_risk, not with severity or line count.",
    "expected_artifacts": {
      "referenced_findings": ["contract_drift"],
      "referenced_files": ["src/api/state.ts", "src/billing.ts"],
      "expected_priority": "contract_drift"
    },
    "judge_questions": [
      "Did the agent rank contract_drift above the structural findings and justify it on agent_risk rather than on severity or file size?",
      "Did the agent name the fields present on one declaration and absent from the other, rather than saying the two declarations 'differ'?"
    ]
  },
  {
    "id": "review-15-duplicated-policy",
    "fixture": "15",
    "kind": "review",
    "prompt": "A PR widens billing export to team owners. Use `crimes scan --format json` to find every place that decision is currently made. Name each site with its file and function, quote the rule as written, and say what happens if the PR changes only the one it was pointed at. Then say whether you would block the PR, and what single change would stop the next agent hitting this.",
    "expected_artifacts": {
      "referenced_findings": ["duplicated_policy"],
      "referenced_files": ["src/routes/export.ts", "src/services/entitlements.ts"],
      "expected_priority": "duplicated_policy"
    },
    "judge_questions": [
      "Did the agent name BOTH sites \u2014 the route and the entitlements service \u2014 rather than only the one the prompt's framing points at?",
      "Did the agent propose collapsing the two into a single policy function, rather than proposing to edit both copies and keep them in sync?"
    ]
  },
  {
    "id": "review-15-agent-config",
    "fixture": "15",
    "kind": "review",
    "prompt": "You have just cloned this repository and are about to open it with a coding agent. Before doing any work, use `crimes scan --format json` to review what the repo's own agent configuration would do to you. Report what runs automatically and what has been pre-approved, quoting the exact rules. Rank what you found by whether it executes or merely asks, and say which items you would refuse to work under until they are changed.",
    "expected_artifacts": {
      "referenced_findings": ["agent_permission_sprawl"],
      "referenced_files": [".claude/settings.json", "AGENTS.md"],
      "expected_priority": "agent_permission_sprawl"
    },
    "judge_questions": [
      "Did the agent separate the executable hazards (the PostToolUse hook piping a remote URL into a shell, and the `Bash(*)` / `Write(/etc/risky/**)` pre-approvals) from the advisory prose in AGENTS.md, rather than treating all of it as one severity?",
      "Did the agent notice that the hook runs without a prompt for anyone who opens the checkout, rather than describing it as something it would be asked about?"
    ]
  },
  {
    "id": "review-16-whole-repo-audit",
    "fixture": "16",
    "kind": "review",
    "prompt": "You have been asked to name the single most dangerous thing in this repository \u2014 not the most recently touched, and not what the team is currently working on. Use `crimes scan --format json`. Name it, quote the evidence, say what a customer would experience if it went wrong, and explain why you ranked it above everything the scan lists near the top.",
    "expected_artifacts": {
      "referenced_findings": ["unsafe_retry"],
      "referenced_files": ["src/legacy/payouts.ts", "src/legacy/settlement.ts"],
      "expected_priority": "unsafe_retry",
      "finding_labels": [
        {
          "type": "unsafe_retry",
          "file": "src/legacy/payouts.ts",
          "symbol": "sendPayout \u2192 fetch",
          "priority": true
        },
        {
          "type": "unsafe_retry",
          "file": "src/legacy/settlement.ts",
          "symbol": "settleBatch \u2192 fetch",
          "priority": true
        }
      ]
    },
    "judge_questions": [
      "Did the agent name the payout retry \u2014 a money movement replayed with no idempotency key, so a timeout after the provider committed pays twice \u2014 rather than picking whichever finding the scan happened to rank first?",
      "Did the agent explain the customer-visible consequence (a duplicate payout) rather than describing the retry as a robustness or style concern?"
    ]
  }
]
