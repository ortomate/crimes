[
  {
    "id": "bugfix-01-messy-ts-app",
    "fixture": "01",
    "kind": "bugfix",
    "prompt": "Imagine the billing tests are failing on a date-boundary edge case. Use `crimes scan src/billing.ts --format json` to find risk factors that could explain the flakiness. Identify the most likely cause and propose a fix.",
    "expected_artifacts": {
      "referenced_findings": ["direct_date"],
      "referenced_files": ["src/billing.ts"],
      "expected_priority": "direct_date"
    },
    "judge_questions": [
      "Did the agent connect the failing date test to direct clock access (Date.now()/new Date())?",
      "Did the agent's fix introduce a clock/now() abstraction rather than monkey-patching globals?"
    ]
  },
  {
    "id": "bugfix-08-stress-dependency",
    "fixture": "08",
    "kind": "bugfix",
    "prompt": "A circular-dependency runtime error is suspected. Use `crimes scan --format json` to find the cycle, then propose which import to break and why.",
    "expected_artifacts": {
      "referenced_findings": ["circular_dependency"],
      "referenced_files": [
        "src/domain/billing.ts",
        "src/presentation/invoice-view.ts",
        "src/application/billing-flow.ts"
      ],
      "expected_priority": "circular_dependency"
    },
    "judge_questions": [
      "Did the agent correctly identify the cycle (domain \u2192 presentation \u2192 application \u2192 domain)?",
      "Did the agent's fix preserve the dependency direction (lower-layer should not depend on higher)?"
    ]
  },
  {
    "id": "bugfix-01-timezone-parse",
    "fixture": "01",
    "kind": "bugfix",
    "prompt": "A promo window test passes in CI (UTC server) but in production the promo opens a day early for European users. Use `crimes scan src/date.ts --format json` to find the parse-time timezone bug, name the literal that's responsible, and propose the smallest change that makes the production behaviour match the CI behaviour without renaming the function.",
    "expected_artifacts": {
      "referenced_findings": ["timezone_unsafe_parse"],
      "referenced_files": ["src/date.ts"],
      "expected_priority": "timezone_unsafe_parse"
    },
    "judge_questions": [
      "Did the agent identify the unzoned literal (e.g. \"2026-12-20\") as the source of the off-by-one-day bug?",
      "Did the agent's fix explicitly add `Z` or an offset (or switch to a timezone-aware parser) rather than papering over the bug with arithmetic?"
    ]
  },
  {
    "id": "bugfix-04-weak-tests",
    "fixture": "04",
    "kind": "bugfix",
    "prompt": "A user is reporting that an esbuild plugin regression slipped through CI even though the relevant test still passes. Use `crimes scan test --format json` to find tests that may not actually be asserting anything, then explain which test you'd strengthen first and what specific assertion you'd add. Don't paraphrase the finding evidence \u2014 quote it.",
    "expected_artifacts": {
      "referenced_findings": ["weak_test_signal"],
      "expected_priority": "weak_test_signal"
    },
    "judge_questions": [
      "Did the agent pick a specific test from the weak_test_signal findings and propose the exact assertion the test is missing?",
      "Did the agent quote at least one finding's evidence verbatim instead of paraphrasing?"
    ]
  },
  {
    "id": "bugfix-11-py-mixed-clocks",
    "fixture": "11",
    "kind": "bugfix",
    "prompt": "Customers in Europe report that invoices created late in the evening are stamped with the wrong billing period \u2014 but only sometimes, and never on our CI runner. Run `crimes scan --format json` on this repo and work out which module is responsible and why the bug is invisible in CI. Explain the mechanism, then propose a fix.",
    "expected_artifacts": {
      "referenced_findings": ["mixed_utc_local_methods", "direct_date"],
      "referenced_files": ["domain/invoicing.py"],
      "expected_priority": "mixed_utc_local_methods"
    },
    "judge_questions": [
      "Did the agent identify that utcnow() and now() are both used in the same module and both return naive datetimes?",
      "Did the agent explain that a UTC-configured CI runner makes the two agree, which is why the bug does not reproduce there?",
      "Does the proposed fix move to timezone-aware datetimes rather than just replacing one call with the other?"
    ]
  },
  {
    "id": "bugfix-13-polyglot-plan-drift",
    "fixture": "13",
    "kind": "bugfix",
    "prompt": "Users on our newest plan tier are being rejected by the web app with a validation error, but the API accepts them fine. Run `crimes scan --format json` and work out why. Explain the mechanism and say what would stop it recurring.",
    "expected_artifacts": {
      "referenced_findings": ["cross_language_type_drift"],
      "referenced_files": [
        "packages/api/billing/plans.py",
        "packages/web/src/workspace-client.ts"
      ],
      "expected_priority": "cross_language_type_drift"
    },
    "judge_questions": [
      "Did the agent identify that the Python Plan enum and the TypeScript Plan union list different members?",
      "Did the agent name the specific values that exist on one side only?",
      "Did the agent propose generating one side from the other (or a shared schema) rather than just adding the missing member by hand?"
    ]
  },
  {
    "id": "bugfix-04-unbounded-fanout",
    "fixture": "04",
    "kind": "bugfix",
    "prompt": "The test helper intermittently fails on CI with EMFILE (too many open files) when a fixture declares a lot of files, but never reproduces locally. Use `crimes scan test --format json` to find the concurrency finding responsible, explain why the failure is load-dependent and therefore machine-dependent, and write the bounded version. Quote the finding's evidence for the fan-out site and the collection it iterates.",
    "expected_artifacts": {
      "referenced_findings": ["unbounded_async_fanout"],
      "referenced_files": ["test/utils.ts"],
      "expected_priority": "unbounded_async_fanout"
    },
    "judge_questions": [
      "Did the agent identify the `Promise.all` over `Object.keys(files).map(\u2026)` as the fan-out, rather than blaming the filesystem or the CI runner?",
      "Did the agent propose an actual bound \u2014 a concurrency limit or batching \u2014 rather than only adding a retry or an error message?"
    ]
  },
  {
    "id": "bugfix-15-unsafe-retry",
    "fixture": "15",
    "kind": "bugfix",
    "prompt": "Support reports that a handful of customers were charged twice during a provider outage last week. Use `crimes scan --format json` to find the code that could do that. Name the retried operation and the file, quote the evidence for why a replay is possible, and write the specific change that makes the retry safe. Say why adding backoff or lowering the attempt count would not fix it.",
    "expected_artifacts": {
      "referenced_findings": ["unsafe_retry"],
      "referenced_files": ["src/services/payments.ts"],
      "expected_priority": "unsafe_retry"
    },
    "judge_questions": [
      "Did the agent identify the missing idempotency key as the defect, rather than the absence of backoff, jitter, or a lower attempt cap?",
      "Did the agent's fix pass a stable key derived from the operation (not a fresh uuid per attempt), so the receiver can recognise the replay?"
    ]
  },
  {
    "id": "bugfix-15-config-drift",
    "fixture": "15",
    "kind": "bugfix",
    "prompt": "`REQUEST_TIMEOUT_MS` is set to 30000 in production and the notify job behaves as though it were never set at all. Use `crimes scan --format json` to explain why. List every read of the variable with its file, the type it is parsed as, and the default it falls back to. Say which read is wrong and what the value 30000 means at each site.",
    "expected_artifacts": {
      "referenced_findings": ["config_drift"],
      "referenced_files": [
        "src/config/index.ts",
        "src/jobs/notify.ts",
        "src/services/payments.ts"
      ],
      "expected_priority": "config_drift"
    },
    "judge_questions": [
      "Did the agent name all three reads and the type each parses to, rather than describing the variable as merely 'used inconsistently'?",
      "Did the agent point out that the payments read treats a _MS-suffixed variable as a small integer while config treats it as milliseconds, so one of the two is off by a factor of a thousand?"
    ]
  }
]
