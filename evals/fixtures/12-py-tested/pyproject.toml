[project]
name = "py-tested"
version = "0.1.0"
description = "Python package with a tested module, an untested one, a hollow test file, and one whose assertions live in a shared helper module. Eval fixture only."
requires-python = ">=3.11"
