# crimes

> crimes is an open-source command-line tool that scans TypeScript,
> JavaScript and Python repositories for change risk and AI-agent risk,
> including disagreements between languages that neither side's own
> tooling can see. It runs
> locally with no cloud calls, ships a stable versioned JSON contract
> so AI coding agents can consume its output safely, and is MIT-licensed.

crimes is positioned narrowly. It is not a linter (ESLint, Biome),
not a security scanner (Semgrep, CodeQL), and not a code-quality
platform (SonarQube). Its wedge is local, open-source, agent-native
codebase risk and context — the question "where is future change most
likely to go wrong, and what should a human or AI agent know before
editing it?".

Core principles:
- Deterministic before magical. Detectors must work without any LLM.
- Evidence before judgement. Every finding cites concrete AST or git
  evidence (line ranges, churn counts, import patterns).
- JSON output is the product contract. Every report carries a
  `schema_version` (currently `"0.7.0"`) and a `report_type`
  discriminator (`"scan"`, `"context"`, `"hotspots"`, `"diff"`,
  `"baseline"`, `"baseline_check"`, `"verdict"`, `"explain"`,
  `"suppressions"`, `"audit_suppressions"`, `"triage"`). Breaking
  changes bump `schema_version`.
- Built for AI agents. Ships a Claude Code skill and AGENTS.md so any
  agent (Claude Code, Codex CLI, Cursor, Aider, Continue, Copilot
  Workspace) can use it without per-prompt setup.

## Install

- npm: `npm install -g crimes`
- one-shot: `npx crimes scan .`
- requires Node.js 18+

## Commands (latest v0.23.0)

`crimes feedback` (shipped in v0.7.0) — capture per-finding verdicts that
feed the calibration loop:
- `crimes feedback <fingerprint> --verdict {tp|fp|known} --note '...'`
  — write a verdict. `fp` requires `--note` and writes a
  feedback-sourced suppression pinned to the current crimes minor.
- `crimes feedback list [--global] [--since 30d] [--verdict tp|fp|known]`
- `crimes feedback summary [--global]` — by_verdict / by_detector /
  by_version / by_repo (only with --global) aggregates.
- `crimes feedback export [--format jsonl|md|json] [--append-global]`
  — append-global pushes local entries to
  `~/.crimes/feedback-rollup.jsonl`, deduped by
  `(repo, timestamp, fingerprint)`.
- `crimes feedback recheck [--detector <type>]` — per-release review
  surface; walks every feedback-sourced suppression whose pinned
  minor is older than the current one, prints the re-feedback
  commands per resurfaced finding.



Per-file / per-directory (shipped in v0.1.0):
- `crimes scan .` — scan repo, print top findings
- `crimes scan . --format json` — emit the versioned JSON contract
- `crimes scan --changed --format json` — working-tree-only post-edit scan
- `crimes context <file> --format json` — pre-edit briefing for one file
- `crimes hotspots --since 90d --format json` — churn × findings ranking

Branch / PR safety (shipped in v0.2.0):
- `crimes scan --changed --fail-on low|medium|high` — exit 1 when the
  working set has a finding at or above the threshold. Requires a
  working set: `--changed`, `--files`, or `--related-to` (all three
  since v0.20.0; `--changed` only before that). JSON gains `fail_on`
  and `failed` fields.
- `crimes diff <base...head> --format json` — new / fixed / unchanged
  crimes between two committed refs (triple-dot range). Working-tree-safe;
  exports each ref via `git archive` into a temp dir. Findings matched
  by stable fingerprint `<type>::<file>::<symbol-or-empty>`.
- `crimes baseline save` — snapshot current findings to
  `.crimes/baseline.json` (intended to be committed).
- `crimes baseline check --fail-on low|medium|high` — re-scan and fail
  only on findings absent from the baseline. Default threshold `medium`.
- `crimes verdict --base origin/main --format json` — one-line "did
  this branch make the repo cleaner, worse, unchanged, or mixed?"
  Advisory by default; opt into a hard gate with `--fail-on worse |
  new-high | new-medium`.

## CI integration (v0.2.0)

Three deterministic gating modes — all share a uniform exit-code
contract: `0` success, `1` configured `--fail-on` threshold met,
`2` usage / environment error.

- Mode A — `crimes scan --changed --fail-on high` (narrow,
  changed-files-only).
- Mode B — `crimes baseline check --fail-on medium` (legacy repo;
  pin debt with a committed `.crimes/baseline.json`).
- Mode C — `crimes verdict --base origin/main --fail-on new-high`
  (PR summary that flips to a hard gate on any new high finding).

Copy-paste GitHub Actions workflow ships in the repo at
`examples/github-actions/crimes.yml`. Full integration guide:
`docs/ci.md`.

## Structural detectors shipped today (v0.1.0)

- `large_function` (charge: God Function) — functions over a body-line threshold
- `large_file` (charge: God File) — files over a line threshold
- `todo_density` (charge: Unfinished Business) — clusters of TODO/FIXME/XXX/HACK
- `direct_date` (charge: Temporal Recklessness) — `Date.now()` / `new Date()`
  used directly in domain code

## Information architecture detectors (new in v0.3.0)

These five IA detectors are new in v0.3.0. They are deterministic:
**no LLM, no API key, and no network access** is required. Every
finding cites concrete files, line numbers, and string literals;
cross-file findings populate `related_files` and render as an "Also
touches:" block in the human report.

- `missing_agent_context` (charge: Missing Agent Context) — repos that
  declare a `bin` in `package.json` but ship no `AGENTS.md`,
  `CLAUDE.md`, or `.claude/skills/*/SKILL.md`.
- `route_metadata_drift` (charge: Route Metadata Drift) — route path,
  file location, default-export component name, `<title>`,
  `metadata.title`, and nav-source labels disagree for the same
  destination (≥3-source quorum).
- `duplicated_navigation_source` (charge: Duplicated Navigation Source)
  — a single internal destination appears in two or more nav-like
  sources with different non-empty labels.
- `concept_alias_drift` (charge: Concept Alias Drift) — multiple
  aliases from a seeded concept group (`team` / `workspace` / `org`;
  `plan` / `subscription` / `tier`; etc.) appear across the product
  surface, each in ≥2 distinct directories. Capped at the three
  strongest groups per scan.
- `docs_code_drift` (charge: Docs-Code Drift) — a markdown doc under
  `docs/` (or a root-level `*.md`) contains a local link that does not
  resolve on disk.

IA findings phrase summaries as "appears to" / "may" — they are
**ambiguity signals**, not claims of semantic truth. The
interpretation belongs to the human or agent reading the report. Full
reference (quorum rules, false-positive notes, suggested fixes):
`docs/finding-types/ia.md`.

Every finding (structural and IA) includes typed scores: `severity`,
`confidence`, and `agent_risk`. Default ranking is by aggregate risk,
so multiple sources of truth, weak tests, and hidden side effects can
elevate a finding even when local severity is low.

## Agent integration

- Claude Code: `.claude/skills/crimes/SKILL.md` auto-loads in any repo.
- Other agents: a top-level `AGENTS.md` is read by OpenAI Codex CLI,
  Cursor, Aider, Continue, and GitHub Copilot Workspace.

Recommended loop for any agent:
1. Before editing a file:   `crimes context <file> --format json`
   (fires automatically under Claude Code when the `0.11.0` PreToolUse
   hook is installed by `crimes init --agents`).
2. Triage existing findings:`crimes triage` — per-finding interactive
   walk; records disposition + reason + owner to `.crimes/triage.json`.
3. After editing:           `crimes scan --changed --format json`
4. Before opening the PR:   `crimes verdict --base origin/main --fail-on new-high`
5. Triaging the repo:       `crimes hotspots --format json`
6. Any new `severity: "high"` finding introduced by the edit is a blocker.

## Shipped in v0.3.0 — information architecture crimes

v0.2.0 made crimes useful for branches, PRs, CI, and agent loops.
v0.3.0 focused on detecting repo structure drift that confuses
humans, coding agents, teams, and customers — source-of-truth and
concept drift across code, routes, navigation, copy, docs, and
ownership. The five IA detectors listed above are the headline slice.
Live on npm.

## Shipped in v0.4.0 — agent context quality and signal-to-noise

v0.4.0 ships no new detectors. It raises the trust ratio of every
detector that already shipped. Headline additions:

- `ContextReport.related_files` — up to ten files an agent should
  read before editing the target, ranked by a deterministic blend
  of IA-finding passthrough, shared path tokens, domain-prefix
  filename matches, and same-directory siblings. Each entry carries
  a `reason` and an ordinal `score`.
- Monorepo / nested-package root auto-detection for `crimes context`
  — walks up to the nearest enclosing `package.json`; explicit
  `--root` still wins.
- Shape-aware `large_function` — per-shape thresholds for `domain`,
  `test_callback`, `react_component`, `page_export`, `route_handler`,
  and `unknown` functions. React pages, route handlers, and test
  callbacks no longer trip the detector at the 60-line domain
  threshold.
- `docs_code_drift` GitHub-relative link allowlist — `../../issues`,
  `../../pull/N`, `../../wiki/Home`, `../../blob/...` and similar
  GitHub-rewritten paths no longer flag as broken local links.
- `ScanReport.changed_files` — `crimes scan --changed --format json`
  emits a top-level array of every file the resolver returned,
  sorted and deduplicated, including files with zero findings.
- `HotspotsReport.history_limited` (+ `history_limited_reason`) —
  set when `git rev-parse --is-shallow-repository` returns true.
  Agents should downweight rankings when the flag is set.
- Top-level `agent_guidance` ordering in `ContextReport` — JSON
  output places `agent_guidance` ahead of `findings`.
- Empty-field self-explanation — `agent_guidance_reason`,
  `related_files_reason`, and `likely_tests_reason` distinguish "we
  searched and found nothing" from "we didn't search".
- `_test.ts` / `_spec.ts` likely-test discovery.

All additions are additive and backwards-compatible. `schema_version`
stays at `"0.1.0"`. Live on npm. Release notes:
https://github.com/ortomate/crimes/blob/main/docs/releases/v0.4.0.md

## Detectors shipped in v0.6.0

Eighteen new detector types, grouped into four categories. All emit
the standard `Finding` shape; cross-file findings populate
`related_files`. Phrasing on the new detectors stays hedged ("appears
to" / "may") — they are signal, not assertions of semantic truth.

Architecture / dependency graph:
- `layer_violation` (charge: Layer Border Crossing) — an import that
  crosses a forbidden boundary defined by `architecture.layers` +
  `architecture.rules` in `crimes.config.json`.
- `circular_dependency` (charge: Tangled Imports) — strongly-connected
  components in the import graph (≥ 2 files).
- `deep_import` (charge: Deep Import Abuse) — imports reaching
  more than `n` segments deep into another package's source tree.
- `high_fan_in_fan_out` (charge: Crowded Module) — files with unusually
  high import fan-in and/or fan-out.

Information-architecture completion:
- `orphaned_destination` (charge: Orphaned Destination) — routes
  declared in nav / IA index without a resolving source / route file.
- `parallel_destination` (charge: Parallel Destination) — two nav-like
  surfaces declaring different routes for the same canonical destination.
- `permission_ia_drift` (charge: Permission IA Drift) — same role /
  permission identifier categorised differently across surfaces.
- `action_label_drift` (charge: Action Label Drift) — same domain
  action labelled differently across surfaces.
- `command_drift_docs_code_drift` (charge: Command Docs / Code Drift) —
  docs reference a `bin` subcommand the CLI no longer implements.

Frontend / UI agent-risk:
- `design_token_escape` (charge: Design Token Escape) — hard-coded
  colours / spacings / font sizes where the repo has a design-token
  system.
- `accessible_interaction_risk` (charge: Hidden Interaction) — JSX
  element handles pointer events but has no accessible label.
- `duplicate_component_shape` (charge: Duplicate Component Shape) —
  two or more components with AST-hash-equivalent JSX shape.
- `responsive_fragility` (charge: Responsive Fragility) — component
  mixing many breakpoint-specific utilities or hard-pixel widths.
- `copy_ia_drift` (charge: Copy / IA Drift, frontend variant) — nav
  label and breadcrumb / page-title disagree on the same destination.
- `visual_regression_review_hint` (charge: Visual Regression Review
  Hint) — a changed line touches JSX in a way the heuristics suspect
  will alter the rendered output.

Duplication:
- `exact_duplicate_block` (charge: Exact Duplicate Block) — function
  bodies with identical AST hashes across two or more files.
- `near_duplicate_block` (charge: Near-Duplicate Block) — function
  bodies with high-similarity AST-hash bags but small deltas.
- `duplicated_role_status_plan_check` (charge: Duplicated Policy
  Logic) — same domain concept (role / status / plan) checked in
  multiple files with different shapes.

Per-finding scores (M2 completion in v0.6.0):
- `scores.blast_radius` — normalised transitive-importer count.
- `scores.churn` — normalised commits-in-window count.
- `scores.test_gap` — three-tier signal from filesystem + import-graph
  test discovery.
- Unified `agent_risk` formula combines all five inputs. Documented in
  `docs/scoring.md`.

Additional v0.6.0 shape: `cli_command_registrar` for `large_function`
— Commander-style `register*Command(program)` wrappers and their
anonymous `.action(...)` callbacks no longer false-positive at the
domain threshold.

Full Starlight-rendered docs at https://crimes.sh/docs/.

## Shipped in v0.5.0 — suppressions, config, and explainability

v0.5.0 is the product-surface release: six new commands and a real
config story without any new detectors.

- `crimes init [--force]` — bootstrap `crimes.config.json` with
  sensible defaults and inline pointers at the new knobs. Refuses to
  overwrite without `--force`. Non-interactive on purpose.
- `crimes ignore <id-or-fingerprint> --reason "…"` — suppress one
  specific finding. Writes `.crimes/suppressions.json`,
  fingerprint-keyed, with a required non-empty `reason`. The file is
  intended to be committed and reviewed in PRs.
- `crimes unignore <fingerprint>` — remove a suppression entry by
  stable fingerprint. Symmetric to `crimes ignore`; supports
  `--dry-run` and `--file <path>`. Empty `suppressions: []` is left
  in place so the file frame is visible to reviewers.
- `crimes audit-suppressions [--format human|json]` — list every
  entry sorted oldest first, with `age_days` and per-entry concerns
  (`stale` > 180 days, `short_reason` < 16 chars, `vague_reason` for
  deferral keywords like `tmp` / `todo` / `wip` / `too noisy`).
  Emits `report_type: "audit_suppressions"`.
- `crimes explain <id-or-fingerprint> [--from <scan.json>]` —
  deterministic long-form rationale for one finding. Prints the
  detector description, a one-paragraph `why_it_matters`, the
  finding's evidence, suggested actions, related files, and the
  verbatim `crimes ignore` command line. No LLM, no network.
- `crimes diff --fail-on new-high | new-medium` — completes the M4
  CI-gate trio. Exits `1` when any new finding meets the threshold.
- `--show-suppressed` on `scan`, `context`, `baseline check`, `diff`,
  `verdict` — re-surfaces matched findings annotated with
  `suppressed: true` and `suppression_reason`.

Config additions (all optional, all back-compat):

- `thresholds.largeFunction.<shape>` — per-shape overrides for
  `domain` / `route_handler` / `react_component` / `page_export` /
  `test_callback` / `unknown`.
- `detectors.enable` / `detectors.disable` — allowlist / blocklist on
  detector ids. Unknown ids exit `2`.
- `ia.aliasGroups` — additive to the built-in `DEFAULT_ALIAS_GROUPS`.
- `suppressions.path` — override the suppressions file location.
- `architecture.layers` / `architecture.rules` — reserved
  placeholder, schema-validated but not consumed in v0.5.0.

Schema additions (all optional / additive — **no `schema_version`
bump**):

- `Finding.suppressed` / `Finding.suppression_reason` (only present
  with `--show-suppressed`).
- `*Report.suppressed_count` on `ScanReport`, `ContextReport`,
  `BaselineCheckReport`, `DiffReport`, `VerdictReport`.
- `DiffReport.fail_on` / `DiffReport.failed` (only present with
  `--fail-on`).
- New report types: `ExplainReport` (`report_type: "explain"`),
  on-disk `Suppressions` (`report_type: "suppressions"`), and
  `AuditSuppressionsReport` (`report_type: "audit_suppressions"`).

Suppressions are applied **before** every `--fail-on` evaluation. A
suppressed finding never trips a gate, regardless of severity or
which gating command is used.

Release notes:
https://github.com/ortomate/crimes/blob/main/docs/releases/v0.5.0.md

## 0.23.0 — the score's missing inputs (current release)

`agent_risk` is the score that makes crimes something other than a
linter, and its heaviest term is the detector's own judgement about how
badly a finding will mislead an agent. 28 of 70 registered detectors
were not making one.

- Silence was not scored as unknown. A detector that set no intrinsic
  fell back to NEUTRAL_INTRINSIC (0.30), which sits below all 29
  expressed agent-signal bases (0.35–0.80). So crimes ranked
  contract_drift, swallowed_error, duplicated_policy,
  permission_ia_drift, unsafe_retry and mock_saturation beneath its own
  most lenient charge — a verdict nobody entered.
- Nothing enforced the field, and the class those findings land in
  (`standard`) has zero members across all 70 detectors, so the
  fallback path was invisible in the class table too.
- The constant meant to prevent that inversion, STRUCTURAL_CEILING =
  0.3, was fitted to a band that does not exist. Rebuilding ce0ccab —
  the commit that chose it — and scanning the exact tree its comment
  cites: sync_io_in_hotpath is 0.20–0.53 against a claimed 0.43–0.53,
  direct_date 0.18–0.51 against 0.51, commented_out_code 0.14–0.41
  against 0.41, and contract_drift, the type the comment says a
  large_file must not outrank, produces zero findings on that tree.
  Every quoted figure is a per-type maximum, and 45% of the
  agent-signal population sat at or below 0.30 on the day the constant
  was chosen.
- INTRINSIC_DEFAULTS declares all 28 in one table, each value anchored
  to a named expressed peer. One table is the point: intrinsics can
  only be calibrated against each other where they can be seen next to
  each other. A gate reads the detector sources and fails when a new
  detector expresses neither, so this cannot re-accumulate.
- NEUTRAL_INTRINSIC deliberately stays at 0.30. Built-ins can no longer
  reach it, so reaching it now means a detector is missing from the
  table; raising it would make that omission harder to notice.

Measured deterministically with the agent-free ranking metric, split by
whether a scenario's labelled answer is a previously-suppressed type:
suppressed types 0.5441 -> 0.6213 (+0.0772, 7 up / 1 down),
always-expressed types 0.4510 -> 0.4458 (-0.0053, 0 up / 22 down). The
second row is displacement rather than regression — those labels were
chosen while the 28 detectors could not surface.

On real repositories no finding is added or removed, and heads move
only where the suppressed detectors fire. Where they do,
over-concentration falls: hono's top-20 dominant-type lift 6.00 ->
2.80, mlflow's 2.85 -> 2.59.

Two things were measured and deliberately not changed. A monotonic
squash of the structural class scores 13 up / 0 down on the
differentiated bucket and clears structural findings out of the top 20
on four of five corpus repos — not taken, because two findings-moving
changes in one baseline make neither attributable. And zulip's "16 of
20 sync_io_in_hotpath" concentration is stale (12 of 20) and mostly the
repo's own mix, not the ranking's: against the population the head is
drawn from, its lift is 1.20.

Schema unchanged at 0.7.0. Scores and ordering move.

https://github.com/ortomate/crimes/blob/main/docs/releases/v0.23.0.md

## 0.22.0 — close the queue

The remediation queue carried since 0.18.0, finished. Seven entries,
every one reproduced before it was touched, and four of the seven
turned out to be wrong about themselves.

- Fingerprint collisions are gone. Four detectors could emit more than
  one finding per (type, file, symbol) with no way to tell them apart,
  so `crimes ignore` on one silenced its neighbours: `large_function`
  and `sync_io_in_hotpath` on a method name repeated across classes in
  one Python module (airflow's operator pattern gives sagemaker.py four
  `execute`s), `commented_out_code` on non-JS files where every block
  in a file shared one fingerprint, and `weak_test_signal` on two
  identically-titled `it(...)` blocks. Zero collisions now on n8n
  packages/cli, zulip and airflow, down from 4, 39 and 184.
- Only ambiguous fingerprints move. A finding whose symbol is already
  unique in its file keeps the fingerprint it has always had: across
  four repos and 7,888 findings, 16 retired and 51 introduced, and hono
  (which had no collisions) is byte-identical. The queue's claim that
  the fix "invalidates every pinned weak_test_signal suppression" is
  wrong, and so was its claim that the collisions were
  `weak_test_signal` at all — zero of zulip's and zero of airflow's
  are.
- A JavaScript syntax error is no longer indistinguishable from a clean
  file. `coverage.warnings[]` gains files_partial_parse for
  language-js. The entry said no public API existed; ts.NodeFlags
  .ThisNodeHasError is public and is read inside a traversal that was
  already happening, at 1262 ms → 1330 ms over n8n's 2,977 files, with
  a false-positive rate of 1 file in 39,177.
- `large_file` still counts blank lines — implemented, measured and
  reverted. The queue's "drops every number 15–25%" is prose: code
  files run 3–9%, and prose has had its own 1000-line budget since
  0.17.0. Dropping blank lines silences the bundled fixture's own
  `large_file` finding and makes a purpose-built eval scenario
  unrankable. What did change is `countNonEmptyLines`, which counted
  every line and is now `countSourceLines`.
- `verdict`'s identical-tree short circuit was fine. The 1762 ms
  against a scan's 929 ms that indicted it is a measurement-order
  artifact: whichever call runs first in a Node process pays ~70–110 ms
  of warm-up, and verdict was always measured first. Reverse the order
  and verdict comes in below scan.
- Two Python classification items reproduced and re-closed with their
  counter-examples named, and `_vendor/` — the Python spelling of
  `vendor/`, used by pip, setuptools and airflow — added to the
  vendored-scope rule.

Schema unchanged at 0.7.0. No field is added, renamed or retyped.

All 14 eval fixtures scan byte-identically between published 0.21.0 and
this build, and the agent-free ranking metric reports no scenario
moved — so the 0.22.0 eval run is a repeat sample of 0.21.0 rather
than a new measurement. claude 0.77 -> 0.81, codex 0.58 -> 0.58. That
settles 0.21.0's claude 0.82 -> 0.77 as noise, and it measures
per-scenario variance directly: 16 of claude's 48 scenarios and 13 of
codex's moved with nothing changed, two of them by a full 1.00.

https://github.com/ortomate/crimes/blob/main/docs/releases/v0.22.0.md

## 0.21.0 — precision, where the false positives were

Four detectors named in an outside field report as producing false
positives. All four re-verified against main first, all four measured on
real repositories before and after, and **three of the four fixes are
not the fix the report asked for** — because the suggested rules did not
survive contact with the files that prompted them.

- `logic_in_comments` matched its domain vocabulary with
  String.includes, so the term `auth` matched "Authored by the Curator"
  and `utc` matched "captured that outcome". Neither file had anything
  to do with authentication or timezones. Matching is now whole-word
  with a closed set of inflections (-s, -ed, -ing, -ies, plus the e-drop
  before -ing/-ed); the allowlist is what separates `owner` + `s` from
  `auth` + `ored`. A plain word-boundary rule was tried first and broke a
  real cal.com finding ("Do not move this before authorization check"),
  so the vocabulary is now spelled out and grouped by concept.
  choreograph 10 -> 7. cal.com 11 -> 11, which is not a null result: two
  substring matches left and two findings arrived that a spurious nearby
  match had been suppressing.
- `direct_date` — the report cited JobDetail.tsx as "9x Date.now(), 4x
  new Date() and essentially all of it is display formatting". Opening
  the file found two poll timeouts of the form
  `if (Date.now() - startedAt >= VIDEO_POLL_TIMEOUT_MS)`, which is
  exactly the risky shape the report says the detector should narrow to.
  So the narrowing landed as evidence and severity, not as a filter:
  DateUse gains `usage: "compared" | "value"`, the evidence names which
  lines decide a branch, and a file whose readings are all values caps at
  medium severity. choreograph 91 -> 91 findings, high 4 -> 1.
- `high_fan_in_fan_out` — the report asked to exempt modules whose
  exports are type-only. The file it was written about exports 24
  interfaces and one const, so that rule fails on its own example. The
  importer side was already in the import graph and never consulted: 32
  of that file's 33 importers write `import type`. Fan-in is no longer
  promoted to medium when >=80% of importers are type-only. The count and
  the finding are unchanged; the evidence now says how many importers
  take types only. The coupling is real and compile-time — change an
  interface and every importer fails to build, loudly, before anything
  ships.
- `name_behavior_mismatch` — `const supabase = await createClient()`
  followed by `supabase.from(...)` builds the thing the function reads
  *through*, so a `get*` function is no longer charged for it. A shape
  rule (bound, then dereferenced, with a constructor-shaped callee), not
  a createClient allowlist — an allowlist is a treadmill. The first
  version was too broad and the corpus caught it: it silently dropped a
  real `fetch` finding, since `const res = await fetch(url); res.json()`
  fits the shape exactly. choreograph 19 -> 7, all twelve removals
  createClient/createAdminClient.

**Nothing became a filter.** Every change here is evidence or severity.
A finding that is noise mid-task can be exactly what an audit run wants,
and the way to serve both is to rank honestly rather than to hide.

Deliberately not changed: five of choreograph's seven remaining
logic_in_comments hits are the term `admin`, four in files whose path
contains admin. A path-aware rule is a judgement call on a sample of one
repository, not a bug, and the release discipline is one detector tuned
at a time.

Schema unchanged at 0.7.0. No fingerprints move, so pinned baselines,
suppressions and triage entries carry over untouched. Severities move on
a handful of findings, in the lenient direction only.

https://github.com/ortomate/crimes/blob/main/docs/releases/v0.21.0.md

## 0.20.0 — scope it to the work

The most valuable way to run crimes was the least discoverable one, and
the default view was describing a report it was not printing. Everything
here traces to one outside field report on a real Next.js project,
re-verified complaint-by-complaint against main before any of it was
acted on. Two of that report's five asks turned out to be already
shipped; a third would not have worked as proposed.

`crimes scan` gains a **working set** — three mutually exclusive ways to
narrow a scan to the files you care about:

- `crimes scan --files a.ts,b.ts` — name them directly. Repo-relative or
  absolute, comma-separated or a repeated flag.
- `crimes scan --related-to src/lib/api.ts` — a file plus its
  import-graph neighbourhood, walked **both** directions: what it imports
  can break it, what imports it is what it can break. `--related-depth N`
  widens the walk (default 1).
- `crimes scan --changed [--base main]` — the post-edit selector.

On the field-report repo, `--related-to` returns 28 findings across 8
files where the bare scan returns 491.

`--fail-on` now works with any of the three rather than only with
`--changed`, so the CI story is available to path-scoped runs.

The resolved set comes back on the JSON report as `working_set`
(`selector`, `seeds`, `depth`, `files`) — for `--related-to` that is the
result of the walk, not the seeds — because a graph walk that silently
included or excluded a file, with no way to check, is not something an
agent can rely on. A path that matched nothing warns on stderr and adds
a `working_set_path_unmatched` coverage warning; without it a typo'd path
produced a report reading "No crimes detected. Suspiciously clean."

Cross-file indexes are always built from the whole repository, so
`blast_radius` on a working-set scan is the blast radius of the file
within the repo, not within the set.

**`--changed` is documented as the post-edit selector.** It works well —
491 findings to 11 on the same repo — but only after the edits exist. An
agent scoping a change it has not made yet is sitting on a clean tree, so
`--changed --base main` correctly returns nothing. That is exactly the
moment `--files` and `--related-to` exist for.

**The headline number now counts what the report shows.** It used to
announce "491 findings across 208 files, 35 high" above a report body
listing 339 across 137 with 22 high — the other 152 were already
classified `tier: "nonDomain"` (via `scopeTiers.nonDomain`, whose first
default entry is `scripts/**`) and already collapsed into a one-line
"Also flagged elsewhere" footer. The summary now counts the domain
findings the body lists and states the rest as `+152 in non-domain
paths`. `--all` still reports the full count because `--all` lists
everything, and `summary.total` in the JSON is untouched — the renderer
is a view, the JSON is the contract.

The summary is also repeated immediately above the report's closing line
on any report long enough to have lost the header. `scan --top 15` on a
209-file repo emits 296 lines, so the header scrolled off and a second
run at `--top 3` was needed just to read the totals. It goes above the
close, not after: the report still ends on `-> Start with crimes context
<file>`, which is the most actionable line an agent gets.

`crimes init --agents` is now the first thing in the README's agent
section. The field report recorded four steps to first run — searching
~/.claude/skills, ~/.claude/commands, ~/.claude/plugins, a filesystem
find, `which crimes` — before guessing `npx crimes@latest`.

Schema: `schema_version` 0.6.0 -> 0.7.0, purely additive
(`ScanReport.working_set`, and the `working_set_path_unmatched` warning
kind). **No fingerprints change**, so every baseline, suppression and
triage entry carries over untouched.

Not done, and recorded as not done: `crimes context --files a,b,c` — the
field report asked for either that or `scan --related-to`, and this
release ships the second; `context()` builds every cross-file index per
call, so nineteen files would mean nineteen index builds. `crimes
context` stays the deep single-file briefing; `scan --files` is the
working-set view.

https://github.com/ortomate/crimes/blob/main/docs/releases/v0.20.0.md

## 0.19.0 — the backlog release

The largest span the project has published: 50 commits, ~30 defect
fixes, four features, two `schema_version` bumps (0.4.0 -> 0.6.0).
Versions 0.18.0 through 0.18.4 were internal eval-baseline markers and
were never published to npm, so everything they carried lands here.

Two JSON migrations:

- **0.5.0** renames `scores.blast_radius_importers` to
  `scores.blast_radius_transitive_importers` and adds
  `scores.blast_radius_direct_importers`. The old name promised "N files
  import this" and delivered the transitive closure — every file that can
  reach it. On the hono corpus `src/utils/mime.ts` has 5 direct importers
  and a closure of 240.
- **0.6.0** adds a required `fingerprint` field to every finding, plus an
  optional `score_rationale`. The fingerprint is the handle `crimes
  ignore`, `unignore`, `feedback` and `triage` all accept, and the JSON
  did not contain it — consumers had to rebuild it from other fields and
  hope their construction matched.

Migration: pinned baseline and suppression entries for twelve more
detectors stop matching and need re-recording — `commented_out_code`,
`weak_test_signal`, anonymous `large_function`, `unbounded_async_fanout`,
`swallowed_error`, `contract_drift`, `logic_in_comments`,
`duplicate_component_shape`, `name_behavior_mismatch`,
`duplicated_role_status_plan_check`, `negative_flag_maze`,
`return_shape_roulette`. For the symbol-bearing ones only the ambiguous
findings move. `crimes feedback recheck` surfaces them with a
per-detector reason.

The install is clean again. npm 11.18+ blocks install scripts by default,
so the only crimes-specific output on a fresh `npm i crimes` was a
security warning asking the user to approve arbitrary code execution —
spent on a seven-line welcome banner npm swallowed anyway. The
postinstall script is removed; bare `crimes` prints the same onboarding.

Detector fixes, measured on real repositories:

- `commented_out_code` matched English prose: airflow 8,019 -> 45
  findings, 7,320 of which were the Apache licence header (41.2% of a
  17,745-finding report). Matching now requires code syntax.
- `parallel_destination` produced 2,819 findings from 134 files, 52.8%
  of n8n's editor-ui package. It is now the first detector to ship gated
  off by default, announced on stderr when it sits out.
- `pass_through_abstraction` fabricated call chains from shared method
  names — n8n reported a 0.98-confidence chain starting at
  `Set.prototype.has` joining four unrelated registries. 7 chains -> 0.
- `cross_language_route_drift` accused PostHog's `/api/projects/*` of
  drifting from a Stripe mock. An orphan is now reported only when the
  backend declares a route sharing its first path segment.
- `scope-class` never-reportable policy is enforced once in `scan`
  rather than per detector: 15 airflow files carrying 44 findings, all
  from the ~50 detectors that predate the check. airflow 44 -> 0.
- `boolean_naming_drift` flagged published API (`Serializer(many=True)`,
  `strip_whitespace`, `fail_fast`) at effort "quick". Now scoped to
  unannotated locals inside a function. drf 7 -> 1, pydantic 34 -> 20.
- `sync_io_in_hotpath` reports one finding per enclosing function:
  airflow span median 8 -> 1 line, max 4,196 -> 185.
- `hotspots` ranked manifest churn first; hono's #1 moves from
  `package.json` to `src/adapter/aws-lambda/handler.ts`.
- Python test detection: nested `test_*` functions are no longer counted
  as tests, `pytest.warns` and `@pytest.mark.xfail` are credited, and a
  repo-wide Python symbol index resolves cross-file assertion helpers
  through the MRO. airflow claimed-silent tests -27.1%.

Features:

- `agent_risk` stops being a length ranking. Severity is no longer an
  input; findings classify as structural / agent_signal / standard with
  a measured ceiling on the first. zulip/zerver top-20 structural 18 -> 0.
- `blast_radius` moves to a log scale with a direct-fan-in tiebreaker;
  the old score saturated at 1.0 on 47% of findings.
- A `Repo-level` section in the human report, so whole-repo findings
  (`dependency_provenance_gap`, `agent_permission_sprawl`) are visible
  instead of being in the JSON and nowhere in the view.
- `score_rationale` carries score derivation, so `evidence` goes back to
  being receipts a reader can check.

Safety fix worth naming: `crimes scan` told users to write
`"detectors": { "enable": ["parallel_destination"] }` when a gated
detector sat out — and `enable` was a pure allowlist, so following that
advice turned off the other 68 detectors and the entire asset pass with
no warning (13 findings became 1). Naming a gated detector in `enable`
is now additive.

Deliberately not changed: `mixed_utc_local_methods` was reported as
unable to fire on modern Python. The premise is wrong — 728 of airflow's
740 `utcnow()` receivers resolve to a timezone-aware wrapper, so matching
them would have produced ~728 high-confidence false positives. Zero
findings is the correct answer, and a regression test pins it.

https://github.com/ortomate/crimes/blob/main/docs/releases/v0.19.0.md

## 0.17.0 — calibration, and the first wire-format change

The first release to move `schema_version`: 0.3.0 -> 0.4.0.

`Finding` gains an optional `discriminator`, folded into the fingerprint
when present and omitted entirely when not. Fingerprints are now
`<type>::<file>::<symbol-or-empty>[::<discriminator>]`.

The hole it closes: three detectors can legitimately report several
findings about one file — `magic_domain_literal_scatter` (one per
scattered literal), `exact_duplicate_block` and `near_duplicate_block`
(one per duplicate group anchored on the same file). Those findings
shared a single fingerprint, so `crimes ignore <fingerprint>` on one of
them silently suppressed the others without telling the user. A
suppression should mean the one finding its author looked at.

Migration: pinned baseline and suppression entries for those three
types stop matching and need re-recording with a fresh `crimes ignore`
plus `crimes baseline save`. `crimes feedback recheck` surfaces them
with the reason. Every other finding type is byte-identical. The loader
accepts the whole 0.1.0-0.4.0 window, so an un-migrated file still
reads.

Also in this release:

- `large_file` gains a `docs` shape — prose (`.md`, `.mdx`,
  `.markdown`, `.rst`, `.adoc`, `.asciidoc`, `.txt`) gets a 1000-line
  budget at low/medium severity instead of the 300-line domain-code
  budget. Reference documentation is supposed to be long. Data formats
  (`.json`, `.yaml`, `.csv`) are deliberately not docs. Configurable as
  `thresholds.largeFile.docs`.
- The scan's two hash-index builders no longer open every candidate file
  at once. Flagged by crimes' own `unbounded_async_fanout` detector on a
  self-scan, and fixed by removing the fan-out rather than by teaching
  the detector to ignore it.
- `exact_duplicate_block` evidence is reproducible run-to-run. Map
  insertion order used to track which file read finished first, so the
  same anchor file reported different hash groups across scans of an
  unchanged tree. Finding identity was always stable, so baselines and
  diff were never affected.
- `dependency_provenance_gap` honours `!` negation in pnpm `packages:`
  and npm/yarn `workspaces` globs, so a package the package manager
  deliberately excludes is no longer compared against a lockfile that
  correctly does not contain it.
https://github.com/ortomate/crimes/blob/main/docs/releases/v0.17.0.md

## 0.16.0 — the correctness and authority slate

Ten detectors that ask what code does when something goes wrong, and
where a repository keeps its truth twice.

Correctness risk (file-local):
- `swallowed_error` (Catch and Release) — a failure caught and discarded,
  or turned into an ambiguous fallback, with no propagation and no record.
- `unsafe_retry` (Double Jeopardy) — a retry around a potentially-mutating
  operation with no idempotency or deduplication key visible.
- `unbounded_async_fanout` (Concurrency Stampede) — `Promise.all` over a
  runtime-sized collection doing per-element I/O with no visible bound.
- `mock_saturation` (Mock Alibi) — a test that replaces every collaborator
  with a behaviourless double and asserts only on those doubles.

Cross-file authority:
- `duplicated_policy` (Policy Doppelgänger) — the same business rule
  implemented independently in two or more production locations.
- `contract_drift` (Contract Split-Brain) — two declarations of one record
  that disagree. Reads TypeScript interfaces, object type aliases, Zod,
  and Valibot.
- `config_drift` (Environment Roulette) — one environment variable parsed,
  defaulted, or required differently across call sites, read past a central
  config boundary, or exposed to a client bundle.
- `pass_through_abstraction` (Abstraction Laundering) — a chain or cluster
  of wrapper functions that forward arguments and add nothing.

Agent hygiene:
- `dependency_provenance_gap` (Phantom Accomplice) — an import with no
  declaring manifest, a manifest/lockfile disagreement, or a specifier that
  resolves differently between installs. Entirely local: no registry is
  contacted, and no claim is made about the packages themselves.
- `agent_permission_sprawl` (Loaded Agent) — repository-local agent
  settings, hooks, and MCP servers granting unrestricted execution or
  running fetched code. Nothing discovered is ever executed; quoted
  fragments are redacted.

Schema stayed 0.3.0 in that release — new `type` values are additive and
fingerprints were unchanged.
https://github.com/ortomate/crimes/blob/main/docs/releases/v0.16.0.md

## 0.15.0 — polyglot IA + monorepo coverage

The release where crimes reports problems no single-language tool can
see. Three cross-language detectors report disagreements *between*
Python and TypeScript:

- `cross_language_route_drift` — the frontend calls a path no backend
  route declares, or the two sides disagree about the HTTP method. A
  missing path fails as a 404 that reads like a data problem; a method
  mismatch fails as a 405 that reads like a permissions problem.
- `cross_language_type_drift` — a closed set declared in both languages
  (a Python Enum, a TypeScript string-literal union) whose members have
  diverged. Whichever side is narrower rejects values the other already
  produces.
- `cross_language_concept_alias_drift` — the same product concept named
  `team` in Python and `workspace` in TypeScript. An agent asked to
  change "the workspace" greps for one name and finds half the code.

In each case every individual file is correct and every type checker
passes. Neither language's linter can see the other half, so these
survive review and reach production.

Cross-language detectors run once per scan rather than once per file,
over the whole parsed corpus. They never fire one-sided (both languages
must be present), only use evidence quotable verbatim, and require real
overlap before calling two same-named things a disagreement. Matching
is on literal path and member strings, not resolved symbols — a
cross-language import graph remains deferred.

`ScanReport.coverage.by_package` reports per-package file counts and
dominant language on monorepos, answering which *package* is the Python
one in a repo the top-level split calls 75% TypeScript. Absent on
single-package repos.

0.15.0 also fixes a bug 0.14.0 shipped: `crimes context` reported no
findings for Python files, because the pre-edit briefing only ran the
universal and language-js packs and parsing a .py file with the
TypeScript parser yields an empty result rather than an error. Anyone
running crimes on Python should upgrade.

Schema stays 0.3.0; `Finding.pack: "cross-language"` and
`coverage.by_package` land additively. Fingerprints unchanged.

Release notes:
https://github.com/ortomate/crimes/blob/main/docs/releases/v0.15.0.md

## 0.14.0 — Python language pack

`crimes scan` now parses `.py` / `.pyi` via `@crimes/language-py` and
reports Python findings alongside JavaScript ones in the same report.
A mixed repo reports
`packs_loaded: ["universal", "language-js", "language-py"]` with a
populated `files_by_language: { js: 412, py: 138 }`.

Eight Python detectors: `large_function.py`, `direct_date.py`,
`mixed_utc_local_methods.py`, `sync_io_in_hotpath.py`,
`boolean_naming_drift.py`, `weak_test_signal.py`,
`circular_dependency.py`, `deep_import.py`. They are written to the
language rather than ported — `direct_date.py` charges naive datetimes
(no JS analogue), `circular_dependency.py` explains an ImportError at
startup rather than a bundling problem, and `sync_io_in_hotpath.py`
escalates inside `async def`, where a blocking call stalls the entire
event loop rather than one worker.

No Python runtime is required at install or scan time. Parsing goes
through a vendored WebAssembly build of tree-sitter-python: no native
addon, no install scripts, and nothing loaded at all in a repo with no
Python files.

Two scoring fixes shipped alongside, both of which otherwise mis-rank
Python against JavaScript. `test_gap` now understands Python's *prefix*
test convention (`test_billing.py` covers `billing.py`), where before
every Python file scored 1.0 — "no test at all" — regardless of
coverage. And `blast_radius` is computed from a real Python import
graph (`__init__.py` packages, relative-dot levels, src-layouts) rather
than being 0 for every Python file; since 0.13.0 that signal is worth
0.20 of `agent_risk`.

Detector ids are qualified per pack (`large_function.py`) so each
language's version is separately addressable in
`detectors.enable` / `disable`, while `Finding.type` stays abstract
(`large_function`) so cross-language grouping, fingerprints, baselines
and suppressions are unaffected.

Schema stays `0.3.0`; `Finding.pack: "language-py"` lands additively.

Release notes:
https://github.com/ortomate/crimes/blob/main/docs/releases/v0.14.0.md

## 0.13.0 — the ranking release

No new detectors; a substantial change to which findings surface first.
`agent_risk` had collapsed into `severity` (correlation 0.79) while
ignoring `blast_radius` (0.06) — the failure PRD §10 says must not
happen. The formula is now
`0.40*intrinsic + 0.20*churn + 0.20*test_gap + 0.20*blast_radius`,
where `intrinsic` is the detector's own agent-risk judgement, which 30
of 48 detectors computed and had discarded every scan. Severity
correlation is now 0.18, blast radius 0.48. `test_gap` no longer scores
markdown/JSON/YAML, which had been pushing docs to the top of scans.
New optional field `coverage.universal_only_by_extension` answers which
language pack would buy the most coverage. `crimes triage` now records
calibration feedback as a byproduct. Rankings move; fingerprints do
not, so baselines, suppressions and triage carry over.

Release notes:
https://github.com/ortomate/crimes/blob/main/docs/releases/v0.13.0.md

## 0.12.0 — universal pack

Universal-pack detectors now run on every file in every repo without
needing an AST. `crimes scan` on Python, Go, Rust, or any non-JS repo
produces real findings (large files, raster assets, localhost/local-path
leaks, duplicate filenames, docs-code drift, missing agent context, TODO
density, commented-out code) plus a coverage banner explaining the gap.
Schema bumps `0.2.0` → `0.3.0`: `Finding.pack` and `Finding.detector_id`
are now required; `ScanReport.coverage` is optional. New CLI flag
`--explain-coverage`. `crimes context <unsupported.rs>` returns
universal-pack findings instead of an empty report. JS/TS behaviour
unchanged.

Release notes:
https://github.com/ortomate/crimes/blob/main/docs/releases/v0.12.0.md

## 0.11.1 — agent-output calibration

Patch on Release B. `crimes hook` now defaults to compact output for
PreToolUse briefings while keeping `--format json` available. `crimes
hotspots --format json` now shows the top 20 rows by default with
`total_files`, `shown_count`, and `hidden_count`; pass `--all` for the
full list. `crimes scan` headers include severity counts. `crimes
--help` points at `crimes init --agents` again.

Release notes:
https://github.com/ortomate/crimes/blob/main/docs/releases/v0.11.1.md

## 0.11.0 — triage as the front door (Release B)

Minor bump. New `crimes triage` command and triage / baseline
resurfacing. Schema bumps `"0.1.0"` → `"0.2.0"` to add two required
`Finding` fields: `effort` (`quick` / `small` / `medium` / `large`) and
`fix_shape` (one-line ≤120-char description of the shape of the fix).
Existing field shapes, names, and semantics unchanged.

- `crimes triage` — per-finding interactive walk. Each prompt asks for
  a disposition (`fix-now` / `fix-this-PR` / `needs-design` /
  `wont-fix` / `scaffolding`), one-line reason, and owner. Persisted
  to `.crimes/triage.json` (intended to be committed, hand-reviewable
  in `git diff`). Supports `--apply <file>` for non-interactive bulk
  application, `--list`, `--clear <fingerprint>`, `--retriage
  <target>`, `--owner <handle>`, `--all`, `--format human|json`.
  Refuses to start interactively in CI / non-TTY (exit 2; hint to use
  `--apply`).
- Triage + baseline resurfacing — `crimes scan` re-runs the relevant
  detector on files referenced by `.crimes/triage.json` or
  `.crimes/baseline.json` when those files appear in the branch diff
  against `config.triage.resurfaceBase` (default `"main"`). Matching
  findings are surfaced with `previously_triaged` /
  `previously_baselined: true` + the original disposition.
- New `crimes scan` flags: `--show-triaged` (include silenced
  dispositions in output), `--gate-needs-design` (count `needs-design`
  toward `--fail-on`), `--gate-resurfaced` (count resurfaced findings
  toward `--fail-on`). Default gate semantics: `wont-fix`,
  `scaffolding`, and `needs-design` are excluded from `--fail-on`;
  resurfaced findings surface as a reminder, not a block.
- PreToolUse hook in `crimes init --agents` — writes
  `.claude/settings.local.json` with a Claude Code PreToolUse Edit
  hook that runs `crimes hook --format compact` before every Edit /
  Write / NotebookEdit. Merge-safe; `--no-hooks` opts out.
  `.agents/settings.local.json` is a forward-looking stub for Codex.
- Schema bump — `Finding.effort` and `Finding.fix_shape` are required
  on every `Finding` in `0.2.0`. Detector defaults live in
  `packages/core/src/detector-defaults.ts`; consumers that ignored
  unknown fields under `0.1.0` see the two new fields appear for free.
- Loader tolerance — `.crimes/baseline.json` and
  `.crimes/suppressions.json` files committed under `0.10.x` still
  load. The first `crimes ignore` / `crimes baseline save` after the
  upgrade rewrites the file with `"0.2.0"`.
- Human renderer secondary scores read as prose (`blast top-quartile
  (11 importers)` instead of `0.72`). JSON numerics in `Finding.scores`
  are unchanged — `blast_radius`, `churn`, `test_gap`, `recency`,
  `agent_risk` still ship as `0–1` floats rounded to two decimals.

No new detectors. Detector count unchanged at 48. `crimes baseline
save` stays in place as the escape hatch.

Release notes:
https://github.com/ortomate/crimes/blob/main/docs/releases/v0.11.0.md

## 0.10.0 — front-door redesign (Release A)

Minor bump. `crimes scan`'s default human output is now
**file-grouped, not severity-grouped**: files sorted by aggregate risk
(churn × test-gap quartile × blast × recency), top findings inline,
the rest behind `--all`. Use `--flat` to get the previous
severity-grouped layout back.

- `crimes context` leads every entry point (banner, `--help`, README,
  agent docs).
- `scores.test_gap` is now a **repo-relative quartile** (`0 / 0.25 /
  0.5 / 0.75 / 1.0`), not the prior fixed three-step `{0, 0.5, 1.0}`
  mapping. Agents that compared `test_gap === 1` must switch to
  `>= 0.75`.
- New optional fields (`Finding.tier`, `Finding.scores.recency`,
  `ContextReport.clues`) are additive. Schema unchanged
  (`schema_version: "0.1.0"` at the time of release).

Release notes:
https://github.com/ortomate/crimes/blob/main/docs/releases/v0.10.0.md

## 0.9.2 — emoji severity glyphs + ortomate move

UX-only patch. No new detectors, no schema change.

- Severity glyphs in the human report. 🚨 high · ⚠️ medium · 🔎 low
  prefix every finding's title line and the severity heading.
  ✅ / ❌ on the `--fail-on` gate line; ✨ on the "no crimes
  detected" empty state. Suppressed when stdout isn't a TTY, when
  `NO_COLOR` is set, or when `--no-color` is passed — JSON output,
  CI logs, and piped invocations stay emoji-free.
- Repository moved from `andrewfantastic/crimes` to
  `ortomate/crimes`. `repository.url` and `bugs.url` in the
  published package now point at the new GitHub org; every
  documentation deep link follows. npm Trusted Publisher config
  moved to the new org in the same cut-over.

Schema unchanged (`schema_version: "0.1.0"`).

Release notes:
https://github.com/ortomate/crimes/blob/main/docs/releases/v0.9.2.md

## 0.9.1 — visible welcome banner on bare `crimes`

UX-only patch. No new detectors, no schema change.

- Bare `crimes` (no subcommand) prints a short welcome banner
  with the version, three first-step commands
  (`crimes init --agents`, `crimes init`, `crimes --help`), and
  a docs link — instead of Commander's long help dump.
  `crimes --help` still renders the full usage output;
  `crimes --version` still prints just the version.
- Post-install message expanded to include the version and the
  same three commands. The script still runs, but npm 7+
  swallows post-install stdout / stderr by default, so the
  bare-`crimes` banner is the reliable surface. Users who want
  to see post-install output need
  `npm install -g --foreground-scripts crimes` or
  `foreground-scripts=true` in `~/.npmrc`.

Schema unchanged (`schema_version: "0.1.0"`).

Release notes:
https://github.com/ortomate/crimes/blob/main/docs/releases/v0.9.1.md

## 0.9.0 — Codex agent discovery + petty crime

Minor bump. One new detector and an expanded agent surface; the
`Finding` wire format is unchanged.

- Codex agent discovery is first-class. `crimes init --agents`
  writes both `.claude/skills/crimes/SKILL.md` and
  `.agents/skills/crimes/SKILL.md`; a new `--codex-skill` flag
  writes only the Codex skill. The `missing_agent_context`
  detector recognises `.agents/skills/*/SKILL.md` as a satisfying
  signal — so a repo with a Codex skill but no `AGENTS.md` /
  `CLAUDE.md` no longer fires.
- New detector `finder_duplicate_filename` (petty). Flags macOS
  Finder / iCloud conflict-copy filenames like `Button 2.tsx`
  that slip into repos as accidental duplicates. Filename-only;
  requires the Finder-style space before the number, so
  `Page2.tsx` and `v2.ts` are not flagged. Medium severity, 0.90
  confidence.
- `crimes explain` rewrite. Output broken into named section
  helpers; new "Likely remedies" block synthesised from the
  finding's `suggested_actions` plus two generic next-steps. The
  `ExplainReport` JSON gains a new `likely_remedies: string[]`
  field.
- `npm install -g crimes` now prints a one-line nudge to run
  `crimes init --agents`. (Removed in 0.19.0 — npm swallowed the
  output, and npm 11.18+ turned the script into an `allow-scripts`
  approval prompt.)

Schema unchanged (`schema_version: "0.1.0"`).

Release notes:
https://github.com/ortomate/crimes/blob/main/docs/releases/v0.9.0.md

## 0.8.1 — calibration patch

Patch on top of 0.8.0. No new detectors, no schema change.

- `boolean_naming_drift` built-in allowlist expanded with eight
  idiomatic state names (`loaded`, `found`, `settled`, `overflow`,
  `typeonly`, `interpolated`, `limited`, `existed`). Pure default-
  tuning; project configs unaffected.
- `scan-assets.ts` refactored into four named helpers
  (`discoverAssetFiles`, `groupDetectorsByExtension`,
  `runDetectorsForAssetFile`, `buildAssetContext`). Behaviour
  byte-identical.
- Crimes-monorepo dogfood: `crimes.config.json` excludes
  `evals/fixtures/**` and `examples/messy-ts-app/**` from the
  asset pass. No effect on downstream users.

Release notes:
https://github.com/ortomate/crimes/blob/main/docs/releases/v0.8.1.md

## 0.8.0 — extended lens

Thirteen new detectors plus one config feature. Each emits the
existing `Finding` shape — no schema bump.

- `detectors.options.<id>` — per-detector exemption values
  (allowlists, threshold tuning). Sits between `detectors.disable`
  (kills the detector everywhere) and `crimes ignore` (suppresses
  one specific finding). Each detector registers its own zod schema;
  typos surface at config-load time.

Date / time family (5 detectors):
- `timezone_unsafe_parse` — `new Date("…")` with no `Z` / `±HH:MM` offset.
- `mixed_utc_local_methods` — UTC + local Date methods on the same receiver.
- `locale_drift` — `.toLocaleDateString()` / etc. with no locale arg.
- `dst_naive_arithmetic` — `+ 86400000` and folded `24 * 60 * 60 * 1000`.
- `date_string_concat` — `"…" + d.dateMethod()` hand-rolled date strings.

Naming-tier family (2 detectors):
- `boolean_naming_drift` — booleans without `is`/`has`/`should`/`can`/…
  prefix (built-in React-state allowlist; user extension via
  `detectors.options.boolean_naming_drift.allowedNames`).
- `singular_plural_type_mismatch` — `users: User`, `invoice: Invoice[]`
  shape disagreement between name and type annotation.

Hot-path / portability family (3 detectors):
- `sync_io_in_hotpath` — `readFileSync` / `execSync` / etc. inside
  route handlers, page exports, React components, or domain
  functions (test / CLI registrar ancestors suppress).
- `hardcoded_local_path` — `/Users/<name>/…`, `/home/<name>/…`,
  Windows `C:\Users\<name>\…` baked into non-test source.
- `hardcoded_localhost` — `localhost:NNNN`, `127.0.0.1:NNNN`,
  `0.0.0.0:NNNN`, `[::1]:NNNN` outside config-style basenames.

Asset family (3 detectors) — first non-source detectors:
- `oversized_raster` — file size against `thresholds.assetWeight.{low,medium,high}Kb`
  (defaults 200 / 500 / 1000 KB).
- `raster_should_be_vector` — PNG / JPEG / GIF whose width AND height
  both fit ≤ 64 px (configurable `iconSizeMax`).
- `svg_with_embedded_raster` — SVGs containing `<image href="data:image/*;base64,…">`.

Asset detectors run a separate second-pass walk over
`**/*.{png,jpg,jpeg,gif,webp,avif,svg}`. Configured via
`assets.include` / `assets.exclude`.

Release notes:
https://github.com/ortomate/crimes/blob/main/docs/releases/v0.8.0.md

## Not shipped yet — do not invoke or document as available

- `crimes ask` / LLM-assisted features — v1+.
- Homebrew tap + standalone binaries — deferred.
- Suppression expiry / owner (`expires_at`, `owner`) — designed but deferred.
- `date_equality_misuse` — deferred to 0.9.0 with type-info work.
- `exported_stop_word_name` — researched and dropped (exports-only
  prototype found zero hits across 500+ files).
- `dead_export` agent-risk variant (edit-distance match against
  live exports) — deferred to 0.9.0; needs eval-run evidence first.
- WebP / AVIF coverage on `raster_should_be_vector` — v1 supports
  PNG / JPEG / GIF only (the in-tree header parser).

## Resources

- Website:        https://crimes.sh/
- Source:         https://github.com/ortomate/crimes
- npm package:    https://www.npmjs.com/package/crimes
- License:        MIT (https://github.com/ortomate/crimes/blob/main/LICENSE)
- PRD (spec):     https://github.com/ortomate/crimes/blob/main/PRD.md
- Agent guide:    https://github.com/ortomate/crimes/blob/main/docs/agent-usage.md
- CI guide:       https://github.com/ortomate/crimes/blob/main/docs/ci.md
- JSON schema:    https://github.com/ortomate/crimes/blob/main/docs/json-schema.md
- IA detectors:   https://github.com/ortomate/crimes/blob/main/docs/finding-types/ia.md
- Sample output:  https://github.com/ortomate/crimes/blob/main/docs/fixtures/messy-ts-app.json
- Roadmap status: https://github.com/ortomate/crimes/blob/main/docs/roadmap.md
